# Authoring Workflows

> Sprint 2 fills this in (after the workflows/ directory split).

zachflow's workflow system is designed for extensibility. Sprint 2 lands the directory structure (`workflows/{sprint,qa-fix,_shared}/`) and this doc covers how to add a new workflow.

For now: see [`.claude/skills/sprint/SKILL.md`](../.claude/skills/sprint/SKILL.md) for the existing sprint workflow definition pattern.
